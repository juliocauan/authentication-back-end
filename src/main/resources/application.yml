server:
    port: 8443

spring:
    datasource:
        url: jdbc:postgresql://postgres:${DB_PORT}/${POSTGRES_DB}?useSSL=true
        username: ${POSTGRES_USER}
        password: ${POSTGRES_PASSWORD}
    jpa:
        open-in-view: false
    mail:
        protocol: smtp
        properties:
            mail:
                smtp:
                    useSSL: true
                    auth: true
                    starttls:
                        enable: true
