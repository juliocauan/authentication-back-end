spring:
    profiles:
        active: test
    datasource:
        url: jdbc:postgresql://localhost:5434/auth-test?useSSL=true
        username: sa
        password: sa
    jpa:
        open-in-view: false
    mail:
        host: localhost
        port: 3025
        username: admin@authentication.test
        password: admin
        protocol: smtp
        properties:
            mail:
                smtp:
                    useSSL: true
                    auth: true
                    starttls:
                        enable: true
